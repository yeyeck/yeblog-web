export const article = (state) => {
  return state.article
}

export const titles = (state) => {
  return state.titles
}

export const comments = (state) => {
  return state.comments
}

export const previous = (state) => {
  return state.previous
}

export const next = (state) => {
  return state.next
}
